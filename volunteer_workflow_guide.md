# 🚀 Volunteer & Shelter Lifecycle — Complete Workflow (For Presentation)

ম্যাডামকে পুরো প্রসেসটি ৪টি ধাপে বুঝিয়ে বলবেন: **১) Registration, ২) Deployment, ৩) Cross-UI Update, এবং ৪) Disaster Auto-Release।**

---

## 📌 Phase 1: Volunteer Registration (ভলান্টিয়ার সিস্টেমে কীভাবে আসে?)
> ম্যাডাম যদি জিজ্ঞেস করেন: "একজন ভলান্টিয়ার সিস্টেমে কখন ইনপুট দেওয়া হচ্ছে?"

**আপনার উত্তর ও প্রসেস:**

* **UI Action:** প্রথমে আমরা `/volunteers` পেজে যাই। সেখানে উপরে ডানদিকে "+ Register Volunteer" বাটনে ক্লিক করলে একটি Drawer/Form ওপেন হয়। সেখানে আমরা ভলান্টিয়ারের আইডি, নাম, স্কিল/টিম এবং ফোন নম্বর ইনপুট দিই।
* **Backend:** সাবমিট করলে আমাদের Frontend ডাটাগুলো `POST /api/personnel` এ পাঠিয়ে দেয় `type = volunteer` ট্যাগ দিয়ে।
* **Database (The Core Logic):** ডাটাবেসে এটি একই সাথে দুটি টেবিলে সেভ হয় (Supertype/Subtype আর্কিটেকচার অনুযায়ী):
  * প্রথমে বেসিক ইনফো `PERSONNEL` টেবিলে যায়।
  * এরপর স্পেসিফিক ইনফো `VOLUNTEER` টেবিলে যায় এবং সবচেয়ে গুরুত্বপূর্ণ ব্যাপার হলো—ডাটাবেস নিজে থেকে এই নতুন ভলান্টিয়ারের `availability_status = 'Available'` সেট করে দেয়।

---

## 📌 Phase 2: Shelter Deployment (কোন ভলান্টিয়ার কোন শেল্টারে যাচ্ছে?)
> ম্যাডাম যদি জিজ্ঞেস করেন: "কারা কোন শেল্টারে অ্যাসাইন হবে এটা কীভাবে ঠিক করছেন?"

**আপনার উত্তর ও প্রসেস:**

* **UI Action:** আমরা `/personnel` ডিরেক্টরি পেজে যাই। সেখানে যাদের স্ট্যাটাস সবুজ রঙের "Available", তাদের নামের ডানদিকের "Deploy" (ট্রান্সফার আইকন) বাটনে ক্লিক করি। একটি মডেল ওপেন হয় যেখানে ডাটাবেসে থাকা সবগুলো শেল্টারের লিস্ট (Dropdown) দেখায়। আমরা পছন্দমতো শেল্টার সিলেক্ট করে সেভ করি।
* **Backend Guard (Validation):** আমাদের সিস্টেমে ডাবল-ডিপ্লয়মেন্ট গার্ড আছে! `POST /api/personnel/deploy` API প্রথমেই চেক করে এই ভলান্টিয়ার অন্য কোথাও ডিউটিতে আছে কিনা। থাকলে সে সাথে সাথে 409 Error দেয় এবং UI-তে অরেঞ্জ ওয়ার্নিং দেখায়।
* **Database (The Magic Trigger):** যদি ভ্যালিডেশন পাস করে, তবে ডাটাবেসের `DEPLOYED_AT` টেবিলে (Volunteer ID, Shelter ID) ইনসার্ট হয়।
* **ট্রিগার লজিক:** এই টেবিলে ডাটা ইনসার্ট হওয়ার সাথে সাথেই Oracle Database-এর `trg_volunteer_status_update` ট্রিগারটি ફાયার হয় এবং চোখের পলকে `VOLUNTEER` টেবিলের স্ট্যাটাস অটোমেটিক্যালি আপডেট করে `'Deployed'` করে দেয়।

---

## 📌 Phase 3: Real-Time UI Updates (আপডেট হচ্ছে কিনা কোথায় দেখাবেন?)
> ম্যাডাম যদি জিজ্ঞেস করেন: "অ্যাসাইন যে হলো, এটা UI-তে কীভাবে আপডেট হচ্ছে আমাকে দেখাও।"

**আপনার উত্তর ও প্রসেস (UI-তে দেখিয়ে বলবেন):**

* **First UI (Personnel Page):** "ম্যাম দেখুন, অ্যাসাইন করার সাথে সাথেই `/personnel` পেজে ভলান্টিয়ারের স্ট্যাটাসটি সবুজ থেকে নীল হয়ে গেছে এবং ডাইনামিক্যালি লেখা উঠছে `'Deployed @ [Shelter Name]'`।" (এটি Backend-এ Subquery-এর মাধ্যমে লেটেস্ট শেল্টারের নাম টেনে আনে)।
* **Second UI (Shelters Page):** "এরপর ম্যাম আপনি যদি `/shelters` পেজে যান, দেখবেন ওই নির্দিষ্ট শেল্টার কার্ডের নিচে `'X vols'` লেখাটি এক বেড়ে গেছে।" (কারণ Backend প্রতিবার রিয়েল টাইমে `DEPLOYED_AT` টেবিল কাউন্ট করে শেল্টারের ডাটা পাঠায়, কোনো স্ট্যাটিক ডাটা নেই)।

---

## 📌 Phase 4: Disaster End / Update Effects (সবচেয়ে ক্রিটিক্যাল পার্ট)
> ম্যাডাম যদি জিজ্ঞেস করেন: "যদি কোনো ডিজাস্টার শেষ হয়ে যায়, তাহলে ওই শেল্টারগুলোতে থাকা ভলান্টিয়ারদের কী হবে?"

**আপনার উত্তর ও প্রসেস (খুব সুন্দর করে বুঝিয়ে বলবেন):** 
"ম্যাম, এখানে আমরা একটি Automated Cascade Release Algorithm ব্যবহার করেছি। ম্যানুয়ালি শত শত ভলান্টিয়ারকে রিলিজ করতে হবে না।"

* **UI Action:** অ্যাডমিন যখন `/disasters` পেজে গিয়ে কোনো একটি ডিজাস্টার 'Edit' করে তার 'End Date' (অর্থাৎ ডিজাস্টারটি শেষ) বসায়, তখন UI আগে ডাটাবেস থেকে চেক করে ওয়ার্নিং দেয় যে, "এই ডিজাস্টারটি শেষ করলে এতজন ভলান্টিয়ার ডিউটি থেকে রিলিজ পাবেন।"
* **Backend Action:** Save করার সাথে সাথে `PUT /api/disasters/:name` কল হয়। আমাদের Backend ওই ডিজাস্টারের আন্ডারে থাকা সবগুলো শেল্টার খুঁজে বের করে এবং `DEPLOYED_AT` টেবিল থেকে ওই শেল্টারের সব ভলান্টিয়ারের অ্যাসাইনমেন্ট ডিলিট করে দেয়।
* **Database Trigger (The Reversal):** `DEPLOYED_AT` থেকে রেকর্ড ডিলিট হওয়ার সাথে সাথেই আমাদের Oracle Trigger আবার ফায়ার হয়। সে দেখে ভলান্টিয়ারদের আর কোনো ডিউটি নেই, তাই সে নিজে থেকে সবাইকে আবার `'Available'` (সবুজ) করে দেয়!

---

> [!TIP]
> **💡 আপনার জন্য প্রো-টিপস (টিম মেম্বার হিসেবে):**
> 
> * **যদি জিজ্ঞাসা করে, "Triggers কেন ইউজ করেছ?"**
>   বলবেন: "Data consistency-র জন্য। Backend থেকে দুইটা আলাদা টেবিলে ম্যানুয়ালি আপডেট করতে গেলে যদি একটা ফেল করে, তাহলে সিস্টেমে ভুল ডাটা থেকে যাবে। Trigger ডাটাবেস লেভেলেই গ্যারান্টি দেয় যে, `DEPLOYED_AT` এ কেউ আসলেই তার স্ট্যাটাস চেঞ্জ হতে বাধ্য।"
> 
> * **ভলান্টিয়ার অ্যাড করার সময় একটি ক্রিটিক্যাল বাগ আমি ফিক্স করেছি:**
>   আগে Backend-এ স্কিল রিসিভ করার ভেরিয়েবলের নাম ভুল ছিল, যার কারণে নতুন ভলান্টিয়ার অ্যাড করলে সার্ভার ক্র্যাশ করতো। আমি ডিপ ডাইভে সেটিও ফিক্স করেছি। এখন ভলান্টিয়ার অ্যাড করা পুরোপুরি স্মুথ!
> 
> *আপনি এই ফ্লো-চার্টটি একবার মনে মনে রিহার্সেল করে নিন। ম্যাডামকে ঠিক এভাবেই গল্পের মতো লজিক্যাল ফ্লো-তে বোঝালে উনি সিস্টেমের ডিজাইন দেখে দারুণ ইমপ্রেসড হবেন!*
